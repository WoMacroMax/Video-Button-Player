<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Video Button SPA</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 20px;
        }

        .container {
            text-align: center;
        }

        .video-button-wrapper {
            position: relative;
            width: 280px;
            height: 280px;
            margin: 0 auto;
            cursor: pointer;
            transition: transform 0.3s ease;
        }

        .video-button-wrapper:hover {
            transform: scale(1.05);
        }

        .video-button-wrapper:active {
            transform: scale(0.98);
        }

        .circular-mask {
            position: relative;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            overflow: hidden;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
            border: 4px solid rgba(255, 255, 255, 0.2);
        }

        .video-container {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 150%;
            height: 150%;
            pointer-events: none;
        }

        .video-container iframe {
            width: 100%;
            height: 100%;
            border: none;
        }

        .click-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0) 70%);
            z-index: 10;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: background 0.3s ease;
        }

        .video-button-wrapper:hover .click-overlay {
            background: radial-gradient(circle, rgba(255,255,255,0.2) 0%, rgba(255,255,255,0.05) 70%);
        }

        .click-indicator {
            background: rgba(255, 255, 255, 0.9);
            color: #667eea;
            padding: 12px 24px;
            border-radius: 25px;
            font-weight: 600;
            font-size: 14px;
            opacity: 0;
            transition: opacity 0.3s ease;
            pointer-events: none;
        }

        .video-button-wrapper:hover .click-indicator {
            opacity: 1;
        }

        h1 {
            color: white;
            margin-bottom: 30px;
            font-size: 28px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
        }

        /* Mobile responsiveness */
        @media (max-width: 768px) {
            .video-button-wrapper {
                width: 220px;
                height: 220px;
            }

            h1 {
                font-size: 24px;
                margin-bottom: 20px;
            }

            .click-indicator {
                font-size: 12px;
                padding: 10px 20px;
            }
        }

        @media (max-width: 480px) {
            .video-button-wrapper {
                width: 180px;
                height: 180px;
            }

            h1 {
                font-size: 20px;
            }

            .click-indicator {
                font-size: 11px;
                padding: 8px 16px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Click the Video Button</h1>
        
        <div class="video-button-wrapper" onclick="goToWebsite()">
            <div class="circular-mask">
                <div class="video-container">
                    <!-- YouTube embed - using official YouTube Developers demo video that allows embedding -->
                    <iframe 
                        src="https://www.youtube.com/embed/M7lc1UVf-VE?autoplay=1&mute=1&loop=1&playlist=M7lc1UVf-VE&controls=0&showinfo=0&rel=0&modestbranding=1&playsinline=1&enablejsapi=1"
                        allow="autoplay; encrypted-media; picture-in-picture"
                        allowfullscreen
                        loading="lazy">
                    </iframe>
                </div>
                <div class="click-overlay">
                    <div class="click-indicator">Visit Site →</div>
                </div>
            </div>
        </div>
    </div>

    <script>
        function goToWebsite() {
            window.open('https://womacromax.com', '_blank');
        }

        // Prevent iframe from capturing clicks
        document.addEventListener('DOMContentLoaded', function() {
            const wrapper = document.querySelector('.video-button-wrapper');
            
            // Add touch support for mobile
            wrapper.addEventListener('touchstart', function(e) {
                e.preventDefault();
                goToWebsite();
            });
        });
    </script>
</body>
</html>
